import Header from "../components/Header";
import { Layout } from "antd";

const Homepage = () => {
    return (
        <Layout>
            <Header />
        </Layout>
    );
};

export default Homepage;
